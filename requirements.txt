requests==2.32.0         # For fetching weather data from OpenWeather API
pandas==2.1.0            # Optional: for handling crop and sensor data (future use)
numpy==1.26.0             # Optional: for calculations or ML
scikit-learn==1.3.2       # Optional: if you integrate ML models for crop prediction
requests==2.32.0
